#include <nrn/plasticity/plasticity_rule.h>

// PlasticityRule is a plain struct with an ops table -- no implementation
// needed here.  This translation unit exists so the build system has a
// .cpp to compile for the plasticity_rule component.
